# Backend API URL — used SERVER-SIDE only (proxy + rewrites).
# On Vercel: leave empty or unset; /api/* goes through Next.js proxy (no CORS).
# On Docker / direct deploy: set to your backend URL for relay path direct streaming.
# NEXT_PUBLIC_API_BASE_URL=https://api.fishxcode.com

# Site metadata
NEXT_PUBLIC_SITE_NAME=New API
NEXT_PUBLIC_SITE_URL=https://www.fishxcode.com
